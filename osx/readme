window title
need to patch 2bwm, and restart,  in config.h
and always on top and ignore
irc notifications
brightness
keyboard brightness
cmus? status
purgeable memory? -- top -S|awk '{print $2}'

vm_stat
launchctl
defaults
sysctl
ioreg can be slow 
system_profiler !slow
pmset
nvram
sw_vers
core utils (df, top, ...)
systemsetup
